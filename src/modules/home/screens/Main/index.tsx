import { Box } from '@chakra-ui/react';

const Home = () => {
  return (
    <Box
      as="main"
      width="100vw"
      height="100vh"
      overflow="hidden"
      color={`#fff`}
      display="flex"
      justifyContent={`center`}
      alignItems="center"
    >
      teste
    </Box>
  );
};

export default Home;
